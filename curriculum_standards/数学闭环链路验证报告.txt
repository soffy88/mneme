========================================================================
  CMM-Math题目 → 教材KU 匹配 + 链路验证报告（§20 数学闭环）
  执行: 2026-06-21
========================================================================

1. 96条题目→KU匹配结果
-----------------------
匹配成功: 86/96 (90%)
未匹配:   10/96 (10%)

  10条失败原因分析:
  - 3条 纯图片题 (<ImageHere>无文字可匹配)
  - 2条 G1错误年级标签 (正方体展开图/钟表，内容高于G1)
  - 2条 G5题文截断/不完整
  - 1条 G6不确定事件 (旧版G6教材可能未收录概率)
  - 1条 G7分式方程 (LLM未找到足够相似KU)
  - 1条 G10分段函数 (关键题文是图片)

  匹配质量抽查（真实匹配样例）:
  ✅ G7 有理数大小比较 → RENJIAO-G7-MATH-S-ku-有理数的概念 + 有理数的大小比较法则
  ✅ G8 全等三角形判定 → SAS/ASA/AAS判定定理（3个KU）
  ✅ G9 圆周角定理     → 弧弦圆心角关系定理 + 圆周角定理及其推论
  ✅ G5 5的倍数特征   → RENJIAO-G5-MATH-X-ku-5的倍数的特征（精确匹配）
  ✅ G4 四则混合运算   → 无括号混合运算 + 含括号运算顺序 + 两位数乘法
  ✅ G11 三角函数诱导  → renjiao-math-g10-a-ku-利用诱导公式求任意角的三角函数值
     （高中跨册合并候选后从G10必修一找到正确KU）

2. 完整链路验证
-----------
  题目（G7·负数定义）: "下列为负数的是（ ）"
  KU匹配: RENJIAO-G7-MATH-S-ku-正数和负数的定义
  KU描述: "像3,50,7.8%这样大于0的数叫正数；像-3,-10,-0.7%这样在正数前加负号的数叫负数。0既不是正数也不是负数。"

  苏格拉底启动:
    POST /v1/socratic/start → session_id, mode="mixed"
    first_question: "请仔细审题，你认为这道题考察的是什么知识点？请先写出已知条件。"

  苏格拉底对话:
    学生: "负数是比0小的数"
    系统: "这道题你再想想，思路是什么？"（SSE流式，红线未泄露答案）

  结束会话:
    POST /v1/socratic/end → outcome="partial", duration_seconds=66

  ⚠️ 链路缺口: end_session 目前不触发 BKT/FSRS 掌握度更新
     kc_mastery 未写入 RENJIAO-G7-MATH-S-ku-正数和负数的定义
     原因: end_session() 只更新 SocraticSession.outcome，无后续算法调用
     影响: FSRS调度和BKT追踪不会基于苏格拉底结果更新（已有缺口，非本次引入）

3. 可扩展性评估（全量22248条CMM-Math题目）
--------------------------------------
  当前基准:
    96条 → 290秒（顺序） = 3.0s/题
    方法: DeepSeek API + 候选KU列表（每年级全量）

  全量估算:
    22248题 × 3s = 18.5小时（单线程）
    22248题 / 10并发 = 1.85小时（10线程）
    预估费用: 22248 × ~¥0.010/题 ≈ ¥222

  技术可行性: ✅
  - 脚本已支持 --grade 分片、--limit 分批
  - 候选KU缓存在内存（2026条，~150KB）
  - 失败题跳过不影响其他（R-1）
  - 注意: 纯图片题（~15-20%）因无文字无法匹配，需图片OCR

  建议批量策略:
    1. 按年级分片: 12个年级 × 1854题平均
    2. 10并发worker: ~12分钟/年级，总1.5小时
    3. 匹配失败的图片题: 标记为 needs_ocr，待后续处理

========================================================================
  DB现状（执行后）:
    wrong_questions 86/96 已关联真实KU（RENJIAO-*）
    kc_mastery 未自动更新（end_session缺口）
    socratic_sessions 创建并记录 outcome/duration
========================================================================
