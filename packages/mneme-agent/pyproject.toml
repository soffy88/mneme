[project]
name = "mneme-agent"
version = "0.1.0"
description = "Mneme Agent — minimal learning loop over MCP tools"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends._legacy:_Backend"
