[project]
name = "mneme-core"
version = "0.1.0"
description = "Mneme Core — mastery gate, BKT, FSRS, deterministic grading"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends._legacy:_Backend"
